@model IEnumerable<DataLoader.CriticRatingLoader>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Rating)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CriticSite.SiteName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MovieDetailLoader.MovieLoader.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Rating)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CriticSite.SiteName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MovieDetailLoader.MovieLoader.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CriticRatingId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CriticRatingId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CriticRatingId })
        </td>
    </tr>
}

</table>
